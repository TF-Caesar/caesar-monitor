{
  "name": "caesar-monitor",
  "version": "0.1.0",
  "description": "A keyless CLI that watches topics and tells you what's NEW, grounded in live web sources via Caesar search.",
  "type": "module",
  "bin": {
    "caesar-monitor": "dist/cli.js"
  },
  "files": [
    "dist",
    "lib",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup",
    "check": "node dist/cli.js check",
    "test": "vitest run"
  },
  "dependencies": {
    "caesar-search": "^0.1.3",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=22"
  }
}
